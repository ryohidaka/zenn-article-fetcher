name: Release Please

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Deploy"]
    types:
      - completed

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      # Uncomment the following after cloning
      - name: No operation
        run: echo "No operation"
# - uses: google-github-actions/release-please-action@v3
#   with:
#     release-type: node
#     package-name: action-template
